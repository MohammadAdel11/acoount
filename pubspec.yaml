name: project_name
description: A new Flutter project.

publish_to: 'none' # Remove this line if you're planning to publish the app.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2  # Update to the latest version
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.0
  firebase_storage: ^12.3.5
  firebase_core: ^3.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
